apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-pes2ug23cs911
  namespace: pes2ug23cs911-beta

 
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: pes2ug23cs911-nginx:1.0
        imagePullPolicy: Never
        ports:
        - containerPort: 80
